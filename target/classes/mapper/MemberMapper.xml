<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scit.project.dao.MemberMapper">
	<insert id="memberJoin" parameterType="member">
		insert into
			member(
				member_id
				,member_pw
				,member_phone
				,member_name
				,member_birth
				,member_email
				,member_gender
				,member_address
				,member_address_detail
			)values(
				#{member_id}
				,#{member_pw}
				,#{member_phone}
				,#{member_name}
				,#{member_birth}
				,#{member_email}
				,#{member_gender}
				,#{member_address}
				,#{member_address_detail}
			)
	</insert>
	
	<select id="memberSelectOne" parameterType="string" resultType="int">
		select
			count(*)
		from
			member
		where
			member_id = #{member_id}
	</select>

</mapper>
